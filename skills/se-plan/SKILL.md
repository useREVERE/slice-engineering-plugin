---
name: se-plan
description: Generate, review, and refine a temporary implementation plan for one briefed slice. Use when the brief is plan-ready and the how is not yet sequenced. Alias of se-plan-loop.
disable-model-invocation: false
---

# Plan

`se-plan` is the short name for `se-plan-loop`. Compose
`skills/se-plan-loop/SKILL.md` and follow it in full. Do not use a lighter
self-review path. There is one planning behavior.

Treat `$ARGUMENTS` as the brief path, conversation task, existing plan, or
autonomy note, and pass them through unchanged.
