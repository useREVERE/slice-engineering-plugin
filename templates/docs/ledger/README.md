# Ledger

Durable briefs, frontiers, and delivery records.

```text
arcs/<slug>/brief.md
```

Use the brief template from the Slice Engineering plugin
(`templates/ledger/arc-brief.md`). Do not store implementation plans here —
plans are temporary.

If `ledger` is `none` in `.slice-engineering/config.yaml`, this directory
should not exist.
