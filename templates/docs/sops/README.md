# Procedures

Repeatable operator and maintenance procedures live here as one SOP per
file. How to decide whether a fact belongs here is
`documentation-placement.md`.

Do not put always-on agent rules here — those belong in `AGENTS.md`.
Do not put architectural decisions here — those belong in `docs/adrs/`.
