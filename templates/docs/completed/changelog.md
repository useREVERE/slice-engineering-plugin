# Changelog

Capability-narrative log of shipped work. This is the durable history
layer above git commits. Newest entries go at the top.

A git commit says what changed in the tree. An entry here says what
became true for a user or operator.

<!-- /se-reflect appends a short entry when a meaningful capability ships.
     Do not rewrite earlier entries. -->
